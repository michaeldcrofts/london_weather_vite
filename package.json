{
  "name": "london_weather_vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest --environment jsdom  --root src/"
  },
  "devDependencies": {
    "jsdom": "^20.0.3",
    "typescript": "^4.9.3",
    "vite": "^4.0.0",
    "vitest": "^0.26.3"
  },
  "dependencies": {
    "typescript-map": "^0.1.0"
  }
}
